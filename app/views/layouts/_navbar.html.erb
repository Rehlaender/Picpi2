<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "#{image_tag('/ico.png')}".html_safe, "/", :class=>"navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categorias<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% @categories = Category.online.where( :online => true ); %>
            <% @categories.each do |type| %>
              <li><%= link_to type.name, "/group/#{type.link}" %></li>
              <% end %>
          </ul>
        </li>
        <li><%= link_to '¿Como comprar?', "/page/howto", :class=>"" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Nosotros', "/", :class=>"" %></li>
        <%= render "layouts/wea" %>
        <li class="bg-high1"><%= render "layouts/carrito" %></li>
      </ul>
    </div>
  </div>
</nav>
